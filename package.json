{
  "name": "sportwebsite",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/check-articles.mjs && astro build && node scripts/audit-seo.mjs",
    "preview": "astro preview",
    "check:articles": "node scripts/check-articles.mjs",
    "audit": "node scripts/audit-seo.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.3.0",
    "astro": "^5.7.0"
  }
}
